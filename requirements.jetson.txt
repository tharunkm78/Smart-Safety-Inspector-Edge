# Jetson Orin Nano (JetPack 5.x) — pip install -r requirements.jetson.txt
# NOTE: Install in this order to avoid build errors

# 1. PyTorch from NVIDIA L4T wheel (NOT standard pip torch)
# Download from: https://developer.download.nvidia.com/compute/redist/jetson-agent/
# Example for JetPack 5.1 / L4T 35.4:
#   wget https://nvidia.box.com/shared/static/... -O torch-2.1.0a0+fbgd19a6-cp38-cp38-linux_aarch64.whl
#   pip install torch-2.1.0a0+fbgd19a6-cp38-cp38-linux_aarch64.whl

# 2. TensorRT (included in JetPack, verify):
#   python3 -c "import tensorrt; print(tensorrt.__version__)"

# 3. OpenCV (pre-built in JetPack)
opencv-python==4.9.0.80

# 4. Ultralytics (from source to ensure CUDA support)
#   git clone https://github.com/ultralytics/ultralytics.git
#   cd ultralytics && pip install -e .

# Core dependencies
numpy>=1.24.0
Pillow>=10.0.0
requests>=2.31.0
scikit-learn>=1.4.0
scikit-image>=0.22.0

# API backend (lighter for edge)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
websockets>=12.0
pydantic>=2.6.0

# Audio
pygame>=2.5.0

# Hardware
JTOP>=4.0.0
RPi.GPIO>=0.7.0

# Utilities
tqdm>=4.66.0
PyYAML>=6.0.1
psutil>=5.9.0
